type Product @model @auth(rules: [{allow: private}]) {
  id: ID!
  dynamicSlug: String!
  productName: String!
}

type Person @model @auth(rules: [{allow: private}]) {
  id: ID!
  dynamicSlug: String!
  fullName: String!
  title: String!
  description: String!
}
 